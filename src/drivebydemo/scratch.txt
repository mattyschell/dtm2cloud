# ssh to vm
# su, then su postgres 
# then psql

create database dtm;
create user dtmread with password 'doitt4pcf';
create user dtmwrite with password 'doitt4pcf';
GRANT ALL PRIVILEGES ON DATABASE dtm to basemapread;
GRANT ALL PRIVILEGES ON DATABASE dtm to basemapwrite;

# psql -d basemap
